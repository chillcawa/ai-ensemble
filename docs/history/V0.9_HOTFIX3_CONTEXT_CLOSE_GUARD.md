# v0.9 hotfix3 — Context close guard

ContextのInstruction編集に未更新draftがある状態で「閉じる」を押した場合、即座に破棄せず確認ダイアログを表示する。

選択肢:
- 全部更新する: 未保存のInstruction draftをすべて確定してContextを閉じる
- 無視して閉じる: draftを保存せず閉じる
- 戻る: Context画面へ戻る

Escape / ダイアログ外クリックも「戻る」と同じ安全側の動作。
未更新がない場合は従来どおり即座に閉じる。
