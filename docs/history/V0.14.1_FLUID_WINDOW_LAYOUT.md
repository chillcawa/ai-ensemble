# AI Ensemble v0.14.1 — Fluid window layout

- Removed the normal window mode's 1200 px content-width cap.
- The workspace now uses all width available beside the Conversation sidebar,
  whether the window is restored, maximized, or fullscreen.
- Fullscreen remains available to remove native window chrome and gain the
  remaining desktop area; its slightly tighter padding is retained.
