# v0.8.1 hotfix2 — Context Set creation UX

- 常設の「新しいContext Set名」入力欄を削除。
- Context Set操作列の右端に `＋ 新規作成` を配置。
- `新規作成` でアプリ中央の入力ダイアログを表示。
- 空欄では作成不可。
- 同名（大文字小文字差を無視）は作成不可で警告。
- Enterで作成、Esc/背景クリック/キャンセルで閉じる。
- 新規Setはhotfix1仕様どおり空のSetとして作成し、既存Setを元にしたい場合は「複製」を使う。
