# v0.9.4 hotfix1 — Timezone + Turn Order

- OS timezone / manual IANA timezone setting
- timestamps remain stored unchanged; conversion is display-only
- applies to Turn log, Conversation sidebar, Archive, Context Library, TextPad paste history
- Turn order can be Old→New or New→Old
- Turn-internal User→AI ordering never changes
- smart latest navigation changes direction with order
- preferences are local UI settings, not Conversation/Context data
