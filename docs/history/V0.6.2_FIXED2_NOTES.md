v0.6.2 fixed2

Fixed the remaining Rust compile error in src-tauri/src/lib.rs: AvailableModelInfo was already constructed before sorting, but a stale tuple-destructuring map remained after sorting. The final result now returns the Vec<AvailableModelInfo> directly.
