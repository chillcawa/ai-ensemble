# v0.9.4 hotfix3 — Turn Header Layout

修正:
- `比較` をTurnヘッダーの同一レイヤーに絶対配置しない
- 上段: Turnヘッダー + `比較`
- 下段: タイムゾーン適用済み時刻
- 画面幅が狭い場合はflex-wrapで折り返す
- `比較` はTurn操作、時刻は受動的メタ情報として分離

Conversation/Turnの保存データには変更なし。
