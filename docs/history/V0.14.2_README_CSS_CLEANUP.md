# AI Ensemble v0.14.2 — README and CSS cleanup

- Updated README from the obsolete v0.11.0 / six-provider description to the
  current nine-provider v0.14 feature set.
- Documented Handoff reference reliability, fluid window layout, fullscreen,
  data flow, distribution cautions, and current limitations.
- Removed `Grok Live` from Next candidates because the live adapter already
  exists.
- Kept the App state-hub refactor visible as a future maintenance task.
- Removed the obsolete v0.5 `.app-shell` width rule that was fully overridden by
  the current sidebar layout.
