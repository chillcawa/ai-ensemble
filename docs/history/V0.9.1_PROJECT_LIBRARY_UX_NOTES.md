# AI Ensemble v0.9.1 — Project / Context Library operational UX

## Context Library
- Referenceタイトル変更
- 詳細 / Preview
- 元ファイル名、ファイルサイズ、MIME、追加/更新日時、概算Token、所属Set表示
- タイトル / 本文 / 元ファイル名検索
- 形式 / 有効状態 / 現在Set所属でフィルタ
- 全所属Context Setを一覧表示
- ファイル再読込
  - 既存Referenceへ差し替え（ID/Set所属を維持）
  - 新しい版として追加（旧版を保持し、旧版と同じSetにも追加）
- 選択SetのReference token占有目安を各有効AIのcontext windowに対して表示
  - 80%超: warning
  - 100%超: danger
  - 自動削除 / 自動要約はしない

## Conversation / Project
- Conversation Sidebarから別Projectへ移動
- Session ContextはConversationと一緒にprojectIdを更新
- Context SetはProject scopedなので移動時に選択を解除

## Rust
- `move_conversation` command追加
- target Project存在確認
- `list_conversations` / `get_conversation_messages` もMappedRowsの寿命を明示化し、v0.9 hotfix1と同系統のE0597を予防

## Scope
Conversation Import / Memoryはまだ入れていない。次の候補はv0.9.2 Conversation Import。
